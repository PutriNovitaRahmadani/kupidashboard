import 'package:flutter/material.dart';

class CatatScreen extends StatelessWidget {
  const CatatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text('Catat'),
      ),
    );
  }
}
